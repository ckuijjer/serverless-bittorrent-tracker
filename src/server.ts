import http from 'http'

export const createServer = () => {
  // const server = http.createServer()

  throw new Error('createServer throws an error')
}

// require('http')
//   .createServer((req, res) => {
//     if (req.url === '/hello') return res.end('world')
//   })
//   .listen(5000)
